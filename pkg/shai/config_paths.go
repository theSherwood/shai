package shai

const (
	// ConfigDirName is the hidden directory that stores workspace-local Shai metadata.
	ConfigDirName = ".shai"
	// DefaultConfigRelPath is the default relative path to the Shai config file.
	DefaultConfigRelPath = ConfigDirName + "/config.yaml"
)
